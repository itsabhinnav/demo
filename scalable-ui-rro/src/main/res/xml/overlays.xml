<?xml version="1.0" encoding="utf-8"?>
<!--
  Full overlay (manifest has no resourcesMap) so panel XML / config / dimens
  replace by resource name on products that already ship those names.

  On Dewd aosp_tangorpro_car, prefer the in-place DewdDynamic patch
  (scripts/patch_dewd_fullpower.py) until CarSystemUI merges panel names —
  see CARSYSTEMUI.md.
-->
<overlay />
