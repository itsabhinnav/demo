# AOSP Car SDK stubs

Populate with:

```bash
./scripts/cuttlefish/fetch-aosp-car.sh
```

That downloads `android.car-stubs.jar` (and related jars) from the same
`aosp_cf_x86_64_auto-userdebug` Android CI build used for Cuttlefish.

See [docs/cuttlefish.md](../../docs/cuttlefish.md).
